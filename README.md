# Telegram Feed Widget с автообновлением через GitHub Actions

Автоматическое обновление постов из Telegram канала 2 раза в сутки!

## 🚀 Как настроить

### 1. Создайте GitHub репозиторий

1. Зайдите на https://github.com и создайте новый **публичный** репозиторий
2. Назовите его, например, `telegram-feed`
3. Не добавляйте README (добавим позже)

### 2. Загрузите файлы в репозиторий

Создайте в репозитории следующую структуру:

```
telegram-feed/
├── .github/
│   └── workflows/
│       └── update-feed.yml
├── fetch_posts.py
├── index.html
├── posts.json (создастся автоматически)
└── README.md
```

**Способ 1: Через веб-интерфейс GitHub**
- Нажмите "Add file" → "Create new file"
- Создайте `.github/workflows/update-feed.yml` и вставьте содержимое
- Повторите для `fetch_posts.py` и `index.html`

**Способ 2: Через Git (если умеете)**
```bash
git clone https://github.com/YOUR_USERNAME/telegram-feed.git
cd telegram-feed
# Скопируйте файлы
git add .
git commit -m "Initial commit"
git push
```

### 3. Настройте GitHub Secrets

1. Перейдите в Settings → Secrets and variables → Actions
2. Нажмите "New repository secret"
3. Добавьте секрет:
   - Name: `CHANNEL_USERNAME`
   - Value: `itmaksimkoya` (ваш канал без @)

**Примечание:** Токен бота НЕ нужен для этого метода! Мы используем веб-скрейпинг.

### 4. Включите GitHub Actions

1. Перейдите в раздел "Actions" в вашем репозитории
2. Нажмите "I understand my workflows, go ahead and enable them"

### 5. Запустите первое обновление

1. В разделе Actions найдите "Update Telegram Feed"
2. Нажмите "Run workflow" → "Run workflow"
3. Подождите ~30 секунд
4. Проверьте что появился файл `posts.json` в репозитории

### 6. Вставьте виджет на Tilda

1. Откройте файл `index.html` из репозитория
2. Найдите строку с `POSTS_JSON_URL` (строка 180)
3. Замените на:
   ```javascript
   const POSTS_JSON_URL = 'https://raw.githubusercontent.com/YOUR_USERNAME/telegram-feed/main/posts.json';
   ```
   Где `YOUR_USERNAME` — ваш логин на GitHub

4. Скопируйте **весь код** из `index.html`
5. В Tilda добавьте блок **T123 (HTML-код)**
6. Вставьте код
7. Сохраните и опубликуйте

## ✨ Готово!

Теперь посты будут обновляться автоматически **2 раза в сутки** (в 00:00 и 12:00 UTC).

### Как работает

1. GitHub Actions запускает скрипт по расписанию
2. Скрипт парсит ваш Telegram канал через веб-версию (t.me/s/itmaksimkoya)
3. Сохраняет последние 3 поста в `posts.json`
4. Ваш сайт читает этот JSON и показывает посты

### Как изменить частоту обновления

Откройте `.github/workflows/update-feed.yml` и измените расписание:

```yaml
# Каждые 6 часов
- cron: '0 */6 * * *'

# Каждый час
- cron: '0 * * * *'

# Раз в сутки в 09:00 UTC
- cron: '0 9 * * *'
```

### Как показывать больше постов

Откройте `fetch_posts.py` и измените:
```python
POSTS_TO_FETCH = 3  # Измените на нужное число
```

## 🐛 Проблемы

**Посты не загружаются**
- Проверьте что GitHub Actions успешно запустился (раздел Actions)
- Проверьте что файл `posts.json` существует в репозитории
- Проверьте URL в `POSTS_JSON_URL` (должен быть raw.githubusercontent.com)

**Старые посты**
- Запустите workflow вручную: Actions → Update Telegram Feed → Run workflow

**Нужна помощь?**
- Проверьте логи в разделе Actions
- Убедитесь что канал публичный (t.me/itmaksimkoya должен открываться без логина)

## 📝 Преимущества этого решения

✅ Полностью бесплатно  
✅ Не нужен бот и токены  
✅ Автообновление по расписанию  
✅ Работает с публичными каналами  
✅ Фото и видео из постов  
✅ Красивый дизайн как у MAX  

## 🔧 Технологии

- GitHub Actions (автоматизация)
- Python + BeautifulSoup (парсинг)
- GitHub Pages (хостинг JSON)
- Vanilla JS (отображение)
